# Minutes-AI
Minutes.AI — single-page web app that turns meeting audio, live mic dictation, or pasted transcripts into clean markdown minutes via Google Gemini. Templates, multi-user local auth, archive view. Saves one .md per meeting to a user-picked folder (File System Access API). Vanilla JS, served by nginx/Docker
