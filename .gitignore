# macOS
.DS_Store
.AppleDouble
.LSOverride

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo

# Logs
*.log
npm-debug.log*

# Dependencies (api/ npm install creates this inside the docker build,
# but if you run `npm install` on the host for testing it ends up here)
node_modules/
api/node_modules/
api/package-lock.json

# Claude Code local-only state
.claude/settings.local.json

# Local Docker Compose override (see docker-compose.override.example.yml)
docker-compose.override.yml
